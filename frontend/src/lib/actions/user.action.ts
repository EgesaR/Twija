'use server';

export const logIn = async () => {
  try {
  } catch (error) {
    console.error('Error', error);
  }
};

export const signUp = async (userData: SignUpParams ) => {
  try {
  } catch (error) {
    console.error('Error', error);
  }
};